$ source env/Scripts/activate
$ pip freeze > requirements.txt
$ pip install --target ./package -r requirements.txt 
$ cd package/
$ zip -r ../deployment-package.zip .
$ cd ..
$ zip -g deployment-package.zip v1beta_lambda.py